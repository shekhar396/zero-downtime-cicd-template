# Mock Artifact

A tiny local artifact used to validate Phase 4 release directory management.

This directory is copied into a generated release directory by `scripts/create-release.sh`. It is not a deployable application.
