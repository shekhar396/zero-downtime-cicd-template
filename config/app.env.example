# Generic application runtime environment copied to <deploy_path>/shared/.env
# during onboarding when no environment file exists yet.

# Application identity and environment label used by the running service.
APP_NAME=zero-downtime-demo-go
APP_ENV=production

# Release metadata default. Deployment scripts and applications may override
# this value at runtime.
RELEASE_ID=local
